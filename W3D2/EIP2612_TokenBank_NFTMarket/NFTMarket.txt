// SPDX-License-Identifier: MIT
pragma solidity ^0.8.13;


import "@openzeppelin/contracts/token/ERC20/extensions/IERC20Permit.sol";

contract NFTMarket {
    IERC20Permit public immutable tokenContract;

    constructor(address _tokenAddress) {
        tokenContract = IERC20Permit(_tokenAddress);
    }

    function buy(address tokenAddress, uint256 tokenId, uint256 amount, uint256 nonce, uint256 deadline, uint8 v, bytes32 r, bytes32 s) public {
        IERC20Permit(tokenAddress).permit(msg.sender, address(this), amount, deadline, v, r, s);
    
        // Transfer the NFT to the buyer

    }
}